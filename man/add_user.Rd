% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{add_user}
\alias{add_user}
\title{Add a user to the local credentials database}
\usage{
add_user(
  dbdir,
  email,
  password,
  pushover_user_key = NULL,
  name = NULL,
  role = "user",
  profile = list(),
  user_id = NULL
)
}
\arguments{
\item{dbdir}{Path to the duckdb file, or an open DBIConnection.}

\item{email}{User email (login id, unique).}

\item{password}{Plain-text password (hashed before storage).}

\item{pushover_user_key}{The user's Pushover user key (encrypted before
storage). Required to deliver the 2FA code.}

\item{name}{Optional display name.}

\item{role}{Optional role string.}

\item{profile}{Optional named list of extra profile fields, stored as JSON and
exposed to the session after login.}

\item{user_id}{Optional explicit user id (defaults to a random token).}
}
\value{
Invisibly, the \code{user_id}.
}
\description{
Insert a user, hashing the password with Argon2id and encrypting
  the Pushover user key with the master key (\code{SHINYMANAGER_KEY}).
  Server-only; never used in webR.
}
