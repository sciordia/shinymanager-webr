% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local-auth.R
\name{local-authentication}
\alias{local-authentication}
\alias{check_credentials_local}
\alias{secure_app_local}
\alias{secure_server_local}
\title{Local (server) authentication helpers}
\usage{
check_credentials_local(db, allowed_roles = NULL)

secure_app_local(
  ui,
  theme = NULL,
  language = "en",
  timeout = TRUE,
  twofa_window = 30,
  head_auth = NULL
)

secure_server_local(
  check_credentials,
  db,
  pushover_app_token = Sys.getenv("PUSHOVER_APP"),
  pushover_title = NULL,
  twofa_window = 30,
  max_attempts = 5,
  remember_device = TRUE,
  device_ttl_hours = 24,
  timeout = 15,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{db}{Path to the duckdb file, or an open DBIConnection.}

\item{allowed_roles}{Optional character vector of roles allowed to log in. If
\code{NULL}, any user in the database may log in.}

\item{ui}{UI of the application (shown once authenticated).}

\item{theme}{Deprecated and ignored. The authentication page is now built
with \code{muiMaterial::muiMaterialPage()} (Material UI), which suppresses
Bootstrap; this argument no longer applies.}

\item{language}{Language used for shinymanager metadata.}

\item{timeout}{For \code{secure_app_local}, logical; whether to include the
inactivity-timeout script. For \code{secure_server_local}, timeout in
minutes (0 disables).}

\item{twofa_window}{Seconds the 2FA code stays valid.}

\item{head_auth}{Tags to add to the authentication page head.}

\item{check_credentials}{Function returned by \code{check_credentials_local}.}

\item{pushover_app_token}{Pushover application (API) token used to deliver
the 2FA code. Per-user keys are stored encrypted in the database.}

\item{pushover_title}{Title of the Pushover notification. If \code{NULL}, a
default title is used.}

\item{max_attempts}{Maximum wrong 2FA attempts before returning to login.}

\item{remember_device}{If \code{TRUE}, a valid device cookie skips 2FA.}

\item{device_ttl_hours}{Lifetime of the device cookie/token, in hours.}

\item{session}{Shiny session.}
}
\value{
\code{check_credentials_local()} returns a validation function that
  takes \code{user} (email) and \code{password} and returns a list with
  \code{result}, \code{expired}, \code{authorized} and \code{user_info}.

\code{secure_app_local()} returns a UI function.

\code{secure_server_local()} returns reactive values with user
  information.
}
\description{
Use these helpers when the application runs on a server (not webR) and you
want credential-based login with a duckdb user store and Pushover two-factor
authentication. The application code is not exposed to the browser, so a real
login form is shown. Passwords are hashed with Argon2id and the per-user
Pushover key is stored encrypted. All heavy dependencies are Suggests.
}
\seealso{
A runnable example app:
  \code{system.file("examples", "local_auth.R", package = "shinymanager.webr")}.
}
