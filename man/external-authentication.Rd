% Generated by roxygen2: do not edit by hand
\name{external-authentication}
\alias{external-authentication}
\alias{secure_app_external}
\alias{check_credentials_external}
\alias{secure_server_external}
\title{External authentication helpers}
\usage{
secure_app_external(
  ui,
  waiting_ui = NULL,
  head_auth = NULL,
  theme = NULL,
  language = "en",
  timeout = TRUE
)

check_credentials_external(
  allowed_users = NULL,
  allowed_domains = NULL,
  require_verified_email = FALSE,
  user_info = NULL
)

secure_server_external(
  check_credentials = check_credentials_external(),
  external_input = "auth0_user",
  logout_url = NULL,
  logout_callback = NULL,
  timeout = 15,
  keep_token = FALSE,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{ui}{UI of the application.}

\item{waiting_ui}{UI shown while waiting for the external identity.}

\item{head_auth}{Tags to add to the authentication page head, typically Auth0
scripts.}

\item{theme}{Alternative Bootstrap stylesheet.}

\item{language}{Language used for shinymanager metadata.}

\item{timeout}{Timeout session in minutes before logout if sleeping. Use 0 to
disable.}

\item{logout_url}{URL to navigate to after shinymanager removes its local
token. For Auth0 this is usually the \code{/v2/logout} URL.}

\item{logout_callback}{Name of a JavaScript function on \code{window} to call
after shinymanager removes its local token. Used when Auth0 logout is handled
by the browser SDK.}

\item{allowed_users}{Character vector of allowed emails or user ids.}

\item{allowed_domains}{Character vector of allowed email domains.}

\item{require_verified_email}{Require \code{email_verified = TRUE} when the
external identity payload contains that field.}

\item{user_info}{Optional function receiving the normalized external identity
and returning extra fields to expose in \code{user_info}.}

\item{check_credentials}{Function returned by \code{check_credentials_external}.}

\item{external_input}{Shiny input id populated by JavaScript with the external
identity. A character email/user id or a list with \code{email}, \code{user},
\code{name}, \code{sub}, and \code{email_verified} is accepted.}

\item{keep_token}{Logical, keep the token used to authenticate in the URL.}

\item{session}{Shiny session.}
}
\value{
\code{secure_app_external()} returns a UI function.
\code{secure_server_external()} returns reactive values with user
information.
}
\description{
Use these helpers when authentication is handled before Shiny, for example by
Auth0 or Cloudflare Access. In this mode shinymanager only creates its session
token and applies lightweight authorization rules.
}
